# Save Multivariable Analysis Results (Internal)

Save Multivariable Analysis Results (Internal)

## Usage

``` r
save_multivariable_result(
  result,
  outcome,
  predictor,
  output_dir,
  format = c("none", "docx", "csv")
)
```
