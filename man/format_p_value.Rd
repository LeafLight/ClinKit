% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/99_utils.R
\name{format_p_value}
\alias{format_p_value}
\title{Format p-value as plotmath expression string}
\usage{
format_p_value(p, name)
}
\arguments{
\item{p}{Numeric vector, p-values to format (typically 0-1)}

\item{name}{Character scalar, p-value prefix, e.g., "p", "P interaction", "P trend"}
}
\value{
\if{html}{\out{<div class="sourceCode">}}\preformatted{Character vector, valid \code{plotmath} expressions, e.g.,
        \code{"p < 0.001"}, \code{"p~\"=\"~3.14\\times10^{-4}"},
        \code{"p~\"=\"~0.03"}
}\if{html}{\out{</div>}}
}
\description{
Returns p-values in common journal formats:
\itemize{
\item p < 0.001  →  "name < 0.001"
\item 0.001 ≤ p < 0.01  →  scientific notation (with ×10⁻ⁿ)
\item p ≥ 0.01  →  keep 3 decimal places
}
Outputs as \code{plotmath} expressions that can be directly used in
\code{ggplot2::annotate()} or \code{ggpmisc::stat_poly_eq()} label parameters.
}
\examples{
format_p_value(0.0001, "p")
}
