% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/99_utils.R
\name{format_p_value}
\alias{format_p_value}
\title{将 p 值格式化为可绘图的表达式字符串}
\usage{
format_p_value(p, name)
}
\arguments{
\item{p}{数值向量，待格式化的 p 值（通常 0–1）}

\item{name}{字符标量，p 值前缀，如 "p"、"P interaction"、"P trend"}
}
\value{
\if{html}{\out{<div class="sourceCode">}}\preformatted{字符向量，\code{plotmath} 合法表达式，例如
        \code{"p < 0.001"}、\code{"p~\"=\"~3.14\\times10^{-4}"}、
        \code{"p~\"=\"~0.03"}
}\if{html}{\out{</div>}}
}
\description{
按期刊常见格式返回：
\itemize{
\item p < 0.001  →  "name < 0.001"
\item 0.001 ≤ p < 0.01  →  scientific notation（含 ×10⁻ⁿ）
\item p ≥ 0.01  →  保留 3 位小数
}
输出为 \code{plotmath} 表达式，可直接用于 \code{ggplot2::annotate()} 或
\code{ggpmisc::stat_poly_eq()} 的 \code{label} 参数。
}
\examples{
format_p_value(0.0001, "p")
}
